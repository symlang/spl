(jbuild_version 1)

(menhir
 ((flags (--explain -v))
  (modules (parser))))

(executable
 ((name test)
  (modules_without_implementation (ast))
  (libraries (sedlex sedlex.menhir menhirLib))
  (preprocess (pps (sedlex.ppx)))))
