(jbuild_version 1)

(menhir
 ((flags (--explain -v))
  (modules (parser))))

(executable
 ((name test)
  (modules_without_implementation (ast))
  (libraries (sedlex_menhir))
  (preprocess (pps (sedlex.ppx)))))
